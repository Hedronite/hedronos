This folder is a Hedronite lab. Run `./install.sh`. When the kernel answers on `127.0.0.1:18800`, treat `vault/` as the workshop notes and https://hedronite.com as the lesson feed. Do not look for k3s, Tailscale, or PDF tomes. If a lab needs a book, open `checklists/tomes.md`. After install: `hedronos` for the student OS; `lab` for the devops toolbox.
